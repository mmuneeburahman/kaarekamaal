<h1>New Member</h1>
<%# <%= form_with model: @user do |form| %> 
<%= form_for(@user, url: url_for(controller: 'users', action: 'makemember')) do |form| %>
<%# <%= form_with(url: "users/update", method: :post, local: true) do |form| %>
  <%= form.hidden_field :is_member, :value => true %>
  <div>
    <%= form.label :cnic %><br />
    <%= form.text_field :cnic %>
  </div>

  <div>
    <p> Are you sure to become a member<p>
    <%= form.submit%>
  </div>
<% end %>